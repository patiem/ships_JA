package model;

import org.testng.annotations.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;
import static org.testng.Assert.*;

public class FleetAdapterTest {
/*
    @Test
    public void givenFleetWhenConvertToJsonThenReturnCorrectJson(){
        List<Ship> mockedMasts = Arrays.asList(new Mast());




        Fleet mockedFleet = mock(Fleet.class);
        when(mockedFleet.getShips()).thenReturn()




        FleetAdapter fleetAdapter = new FleetAdapter(mockedFleet);

        String fleetJson = fleetAdapter.convertToJson(mockedFleet);


    }

    private List<Ship> createShips(){
        List<Ship> ships = new ArrayList<>();

        ships.add(new Mast(0, 0, 1));

        return ships;
    }
    */
}